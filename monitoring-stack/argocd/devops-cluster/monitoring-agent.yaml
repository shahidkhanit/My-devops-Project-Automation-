apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: monitoring-agent-devops
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://grafana.github.io/helm-charts
    chart: k8s-monitoring
    targetRevision: 1.4.6
    helm:
      valueFiles:
        - $values/charts/agent/devops-cluster-values.yaml
  sources:
    - repoURL: https://github.com/your-org/monitoring-stack
      targetRevision: HEAD
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true